# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:

  pull_request:


  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"; self hosted runner allow more control, versus GitHub hosted runners
  # https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners 
  hello-world:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: let's see if this the actions checkout uses can have a name 
        uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run:  |
          echo Hello, world!
          echo $PATH;

  experimental-world:

    runs-on: ubuntu-latest

    steps:
      - name: environmental variables output
        run: |
          echo "added 030422 to try causing GitHub action on every push or PR to any branch"
          echo Add other actions to build,
          echo test, and deploy your project.
          echo "here is the path: " $PATH
          echo "now for the environmental variables";
          printenv 


  sonarqube-branch-scan:
    name: scan branch for sonarqube issues

    runs-on: ubuntu-latest

    steps:
      - name: this step just has a uses for actions checkout v 2
      - uses: actions/checkout@v2

      - name: this step has several echos

        run: |
          echo "date and time: "
          echo | date
          echo current github action is ${{github.action}}
          echo github acitons path is ${{github.action_path}}
          echo here is the secret of ATLAS_VAULT_ROLE_ID ${{ secrets.ATLAS_VAULT_ROLE_ID }}
          echo it should be <<thisIsTheVaultRoleIDToken>> but probably will be ***





            









